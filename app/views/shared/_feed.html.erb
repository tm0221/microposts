<% if @feed_items.any? %>
  <ol class="microposts">
    <% @feed_items.order('id desc').each do |micropost| %>
    <%= render partial: 'microposts/micropost', locals: { micropost: micropost } %>
    <% end %>
  </ol>
  <%= paginate @feed_items %>
<% end %>
