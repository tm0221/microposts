<span id="favorite_links<%= micropost.id %>">
<% if current_user.favorites.find_by(micropost_id: micropost.id) %>
  <%= render 'users/unfavorite_link', micropost: micropost %>
<% else %>
  <%= render 'users/favorite_link', micropost: micropost %>
<% end %>
</span>